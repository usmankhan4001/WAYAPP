You are tasked with creating a new API endpoint.
- Ensure the endpoint is created under `src/app/api/`.
- Must use `requireAuth` for authentication.
- Provide proper error handling (no empty `.catch(() => {})` blocks).
- Do not use inline styles if returning any HTML.
