You are tasked with debugging the analytics module in this Next.js 15 application. 
Review `src/app/analytics/` and related API endpoints.
Focus on data accuracy and performance bottlenecks.
Ensure you follow the project's strict rules regarding error handling and styling (no inline styles, requireAuth, no empty catch blocks).
